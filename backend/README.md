# Backend

Python backend for the Trading Intelligence Platform.

## Planned Structure

- `models/` — domain models and data contracts
- `pipelines/` — data transformation pipelines
- `engines/` — analytical engines
- `storage/` — persistence
- `api/` — local API boundaries
- `config/` — configuration loading
- `utils/` — shared utilities
- `tests/` — backend tests
